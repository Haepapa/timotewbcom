/**
 * Returns the provided text.
 *
 * This function simply returns the input string without any modifications.
 *
 * @param text - The string to echo.
 * @returns The same string that was passed into the function.
 */
export default function echo(text: string) {
  return text;
}
